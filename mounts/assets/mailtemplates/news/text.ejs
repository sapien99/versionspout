<% for(let i=0; i < versions.length; i++) { %>
<%= versions[i].subject %> (<%= versions[i].semver %>)
    <% for(let j=0; j < versions[i].tags.length; j++) { %>
    <%= versions[i].tags[j].tag %>
    <%= versions[i].tags[j].published %>    
    <% } %>
 <% } %>
